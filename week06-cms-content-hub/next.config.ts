import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: "export",
  images: { unoptimized: true },
  basePath: "/frontend-development-internship-portfolio/week06-cms-content-hub/out",
};

export default nextConfig;
